# Like-n-post
This repo is technical task for job interview.

# Run server
`docker-compose up` to run server. Server should be available at `0.0.0.0:8000`

# Run tests
`./run_tests`

# Run bot
`./run_bot`

Set configuration in `rest-api/bot_con/conf.yml`
